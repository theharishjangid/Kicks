import React from "react";

const Login = () => {
  React.useEffect(() => {
    window.scrollTo(0, 0);
  }, []);
  return (
    <React.Fragment>
      <h2>Login</h2>
    </React.Fragment>
  );
};

export default Login;
