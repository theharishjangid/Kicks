import React from "react";

const Checkout = () => {
  React.useEffect(() => {
    window.scrollTo(0, 0);
  }, []);
  return (
    <React.Fragment>
      <h2>Checkout</h2>
    </React.Fragment>
  );
};

export default Checkout;
